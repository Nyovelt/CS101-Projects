
#include <iostream>
#include <cstdio>

int MAXN = 5e5;
int head[MAXN], to[MAXN], nxt[MAXN];
int cnt;

int main(){
	memset(head, -1, sizeof(head));	
	memset(head, -1, sizeof(to));	
	memset(head, -1, sizeof(nxt));
	int n;
	scanf("%d", &n);
	for(int i=1,a,b,c;  i<n; i++)	
}
